filebeat.modules:
- module: system
  syslog:
    enabled: true
  auth:
    enabled: true

filebeat.inputs:
- type: log
  enable: true
  paths:
    - /var/log/suricata/eve.json

output.logstash:
  host: ["logstash:5044"]